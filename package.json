{
  "name": "private-chat-app",
  "version": "1.0.0",
  "private": true,
  "description": "Ephemeral two-user socket chat — combined deploy",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm --prefix server install --include=dev && npm --prefix client install --include=dev",
    "build": "npm run install:all && npm --prefix client run build",
    "start": "NODE_ENV=production node server/index.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  }
}
